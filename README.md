# ansible_roles

Ansible 自动化部署角色集合。

## 角色目录

| 目录 | 说明 |
|------|------|
| `mysql_deploy` | MySQL 8.0 主从集群部署（二进制包方式），含 mysqldump / XtraBackup 备份 |
| `seaweedfs_deploy` | SeaweedFS 分布式文件存储集群部署（Master、Volume、Filer、S3 Gateway） |
